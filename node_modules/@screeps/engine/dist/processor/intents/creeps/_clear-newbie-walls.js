'use strict';

var _ = require('lodash'),
    utils = require('../../../utils'),
    driver = utils.getDriver(),
    C = driver.constants;

module.exports = function (roomObjects, bulk) {

    _.forEach(roomObjects, function (i) {
        if (i.type == 'constructedWall' && i.decayTime && i.user) {
            bulk.remove(i._id);
            delete roomObjects[i._id];
        }
    });
};
//# sourceMappingURL=../../../sourcemaps/processor/intents/creeps/_clear-newbie-walls.js.map
