 } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

var _ = require('lodash'),
    utils = require('../../../utils'),
    driver = utils.getDriver(),
    C = driver.constants,
    movement = require('../movement');

module.exports = function (object, intent, roomObjects, roomTerrain, bulk, bulkUsers, roomController, stats, gameTime) {

    if (object.type != 'creep') {
        return;
    }
    if (object.spawning || object.fatigue > 0) {
        return;
    }
    if (_.filter(object.body, function (i) {
        return i.hits > 0 && i.type == C.MOVE;
    }).length == 0) {
        return;
    }

    if (!intent.direction) {
        return;
    }

    var d = utils.getOffsetsByDirection(intent.direction);

    if (!d) {
        return;
    }

    var _d = _slicedToArray(d, 2),
        dx = _d[0],
        dy = _d[1];

    if (object.x + dx < 0 || object.x + dx > 49 || object.y + dy < 0 || object.y + dy > 49) {
        return;
    }

    var targetObjects = _.filter(roomObjects, { x: object.x + dx, y: object.y + dy });

    if (!_.any(targetObjects, function (target) {
        return _.contains(C.OBSTACLE_OBJECT_TYPES, target.type) && target.type != 'creep' || target.type == 'rampart' && !target.isPublic && object.user != target.user;
    })) {

        movement.add(object, dx, dy);
    }
};
//# sourceMappingURL=../../../sourcemaps/processor/intents/creeps/move.js.map
